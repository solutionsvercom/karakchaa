{
  "name": "karakchaa",
  "private": true,
  "description": "Hostinger entry — runs API + digital menu + admin from backend/",
  "scripts": {
    "build": "npm install --prefix backend && npm run build --prefix backend",
    "start": "npm start --prefix backend"
  },
  "engines": {
    "node": ">=18"
  }
}
